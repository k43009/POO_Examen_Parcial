# Examen POO — Karen Trejo

## Archivos clave
- `SentinelLinkedList.h`: PushFront/PopFront en O(1) + `GetCount() const`.
- `LQueue.h`: Cola basada en la lista centinela.
- `LStack.h`: Pila con `std::forward_list`.
- `Figuras.h`: Figura, Círculo, Cuadrado, FiguraNLados, Cubo, Línea.
- `ExamMain.cpp`: pruebas.

## Compilar
g++ -std=c++17 ExamMain.cpp -o exam

./exam
